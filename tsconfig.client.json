// tsconfig.client.json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "client" /* Specify the root directory of input files. Use to control the output directory structure with --outDir. */,
    "outDir": "build/client",
    "jsx": "react"
  },
  "exclude": ["server"]
}
